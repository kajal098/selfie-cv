<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">        
        <%= link_to "", new_admin_marketiq_path, class: "btn btn-primary btn-xs pull-right glyphicon glyphicon-plus", title: "Add Marketiq"%>
        <h3 class="panel-title">Marketiqs</h3>
      </div>
      <div class="panel-body">
        <% if @assets.count > 0 %>
          <%= datagrid_table(@report, @assets) %>
          <%= paginate @assets %>
           <%= page_entries_info @assets %>              
        <% else %>
          <p class="alert">There are no results to display . </p>
        <% end %>
      </div>
    </div>    
  </div>  
  
</div>