<%= simple_form_for [:admin, @device], html: { class: 'form-horizontal' } do |f| %>
  
  <%= f.input :name %>

  <div class="form-group clearfix">
    <div class="col-sm-offset-3 col-sm-9">
      <button type="submit" class="btn btn-primary btn-sm">Save</button> <%= link_to "Back", admin_devices_path, class: "btn btn-default btn-sm" %>
    </div>
  </div>
<% end %>